# secrets/template.env
# OPTIONAL: For environment variable approach
# Copy this file to ~/.config/nix-config/local/secrets.env and source it
# This file should NOT be committed to git

# Git Configuration (used if local/secrets.nix doesn't exist)
export GIT_USER_NAME="Your Name"
export GIT_USER_EMAIL="your.email@example.com"

# GitHub
# Run: gh auth login (will be prompted on first setup)

# AWS
export AWS_PROFILE="default"
export AWS_REGION="us-east-1"
# Run: aws configure sso (will be prompted on first setup)

